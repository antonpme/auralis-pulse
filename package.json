{
  "name": "auralis-pulse",
  "version": "1.0.0",
  "description": "Claude Code companion for Windows - context monitoring, permission forwarding, compact trigger",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-autostart": "^2"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "vite": "^6"
  }
}
